
import 'package:flutter/material.dart';

class WorldMissionPage extends StatelessWidget {
  const WorldMissionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('World Mission'),
      ),
      body: const Center(
        child: Text('World Mission Page'),
      ),
    );
  }
}
