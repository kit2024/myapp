
import 'package:flutter/material.dart';

class MinistryPage extends StatelessWidget {
  const MinistryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Ministry'),
      ),
      body: const Center(
        child: Text('Ministry Page'),
      ),
    );
  }
}
