
import 'package:flutter/material.dart';

class WhcPage extends StatelessWidget {
  const WhcPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('WHC'),
      ),
      body: const Center(
        child: Text('WHC Page'),
      ),
    );
  }
}
