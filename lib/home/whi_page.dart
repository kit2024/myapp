
import 'package:flutter/material.dart';

class WhiPage extends StatelessWidget {
  const WhiPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('WHI'),
      ),
      body: const Center(
        child: Text('WHI Page'),
      ),
    );
  }
}
