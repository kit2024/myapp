
import 'package:flutter/material.dart';

class GhpPage extends StatelessWidget {
  const GhpPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('GHP'),
      ),
      body: const Center(
        child: Text('GHP Page'),
      ),
    );
  }
}
